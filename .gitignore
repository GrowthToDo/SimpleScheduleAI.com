# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.local
.env.production

# MCP config holds plaintext API credentials - never commit (use .mcp.json.example)
.mcp.json

# macOS-specific files
.DS_Store

# Playwright MCP session files
.playwright-mcp/

pnpm-lock.yaml

.astro

# local machine config + scratch (kept on disk, out of the repo)
.tmp/
.claude/settings.local.json
.claude/*.lock

# unused QA artifacts (kept on disk, out of the repo)
public/qa-checklist.png
public/quality-checks.png
# Local-only style-board preview (served by dev server, never deployed)
public/design-preview.html
pool-originals/
