# Migration Dry-Run Decision Log

Service-mode to product-mode conversion, dry-run only. Sources untouched in `src/data/post/`.
Outputs in this directory only. Per `docs/seo/positioning-registry.md` mapping table + authoring
rules, and `docs/seo/facts-dossier.md#product-truths` guardrails.

Action key: `TOKEN:<id>` = exact registry row applied · `JUDGMENT` = positioning-bearing phrase
converted with no exact registry row, logged per rule 2 · `GAP-NO-TOKEN` = a phrase that is clearly
positioning-bearing, recurs, and probably deserves its own registry row but was hand-converted here
· `PREMISE-COLLAPSE` = the section's/post's argument structurally depends on the service-vs-software
distinction and does not survive a token swap.

---

## Post 1: shiftwizard-vs-managed-service.md

| Location | Original phrase | Action | Replacement | Note |
| --- | --- | --- | --- | --- |
| Title / H1 topic / slug | "ShiftWizard vs. Managed Scheduling Service" | PREMISE-COLLAPSE | (kept title unchanged in this dry-run body; see proposal below) | The post's category thesis is "self-serve platform vs. managed service" as two distinct product categories. In product mode SSAI is software, not a service, so "managed service" as the second category becomes internally inconsistent with SSAI's own self-label even though other real managed-scheduling-service vendors still exist as a market category. Recommend retitle; see Title/Slug Recommendation below. |
| Intro para 1 | "an AI-native nurse scheduling service built for Texas Critical Access Hospitals... the AI builds the schedule, our team checks it, the nurse manager approves it" | TOKEN:self-label + TOKEN:triad | "AI-native nurse scheduling software... the AI builds the schedule, 21 automated rule checks validate every draft, the nurse manager approves it" | Direct registry rows `self-label` + `triad`. |
| KT bullet 2 | "Self-serve platforms and managed services are not feature-equivalent products" | JUDGMENT | "Self-serve platforms and a guided-setup scheduling software are not feature-equivalent products" | No exact token; "managed services" (plural, generic category noun) recast as the product-mode noun phrase consistent with `no-it`/`onboarding` framing. Kept general "managed service" competitor category alive implicitly by not asserting it no longer exists — just removed SSAI's identity from that bucket. |
| KT bullet 4 | "Managed services handle configuration and weekly schedule generation through a specialist outside the hospital." | JUDGMENT | "A guided-setup scheduling software maps unit rules with the nurse manager once during onboarding, then runs the weekly schedule generation automatically." | Applies the `no-it` founder rule: never imply solo rule-configuration OR an external specialist team; guided setup session is the sanctioned framing. |
| H2 "What Is a Managed Scheduling Service?" | heading | JUDGMENT | "What Is a Guided-Setup Scheduling Software?" | Heading renamed; TOC entry updated to match exactly (anchor integrity rule). |
| Body under that H2 | "The vendor's specialist team handles configuration, weekly schedule generation, and ongoing system maintenance." | JUDGMENT | "The vendor maps unit rules with the nurse manager once during onboarding; after that, the system generates the weekly schedule on its own." | Same `no-it` founder rule as above: no external specialist-team framing survives in product mode; must be guided setup + automation. |
| Body under that H2 | "an external specialist team does the scheduling work and your nurse manager reviews their output" | JUDGMENT | "the system does the recurring scheduling work after a single rule-mapping session, and your nurse manager reviews the output" | Same as above. |
| H2 "Where Does a Managed Service Fit?" | heading | JUDGMENT | "Where Does a Guided-Setup Software Fit?" | Heading + TOC + all in-body references renamed consistently. |
| "no dedicated IT capacity" bullet | "Managed services typically deliver auto-logged audit trails... rather than requiring the nurse manager to maintain documentation through configuration." | TOKEN-adjacent JUDGMENT | "A guided-setup software typically delivers auto-logged audit trails... rather than requiring the nurse manager to maintain documentation through configuration." | Noun swap only; underlying claim (auto-logged trail) unaffected by mode. |
| H2 "How SimpleScheduleAI Fits If You Need the Managed Service Model?" | heading | JUDGMENT | "How SimpleScheduleAI Fits If You Need the Guided-Setup Model?" | Heading + TOC renamed. |
| Body | "SimpleScheduleAI is an AI-native nurse scheduling service built for Critical Access Hospitals... Setup takes 3-5 days from an Excel roster upload." | TOKEN:self-label + TOKEN:onboarding | "...nurse scheduling software... Setup takes 3-5 days from an Excel roster upload, through a guided setup session that maps your unit rules once." | Combined self-label token + onboarding token's guided-setup language. |
| Body | "The AI builds the schedule and our team checks it" | TOKEN:triad | "The AI builds the schedule and 21 automated rule checks validate every draft" | Direct token. |
| Body | "See the AI build a schedule... in the live simulator" | TOKEN:cta-primary (adjacent) | "...in the interactive simulator" | Founder rule: "interactive demo/simulator" not "live demo". |
| Image alt text | "SimpleScheduleAI nurse scheduling managed service for critical access hospitals" | TOKEN:self-label | "SimpleScheduleAI nurse scheduling software for critical access hospitals" | Alt text is positioning-bearing per registry rule 3 caution; converted directly since it already carried the mode label. |
| Body | "SimpleScheduleAI is an AI-native nurse scheduling service for Critical Access Hospitals in Texas. We are not a self-serve scheduling platform... we are not positioned as a head-to-head replacement for ShiftWizard." | TOKEN:self-label + JUDGMENT | "...nurse scheduling software... We are not a self-serve scheduling platform in the ShiftWizard sense; we are not positioned as a head-to-head replacement for it either." | Self-label token; the "not a self-serve platform" contrast preserved as a factual distinction (SSAI still isn't a self-serve, user-configured platform in the ShiftWizard sense — it is guided-setup, browser-based). Reworded to avoid implying SSAI is "software" full stop with no distinguishing model, since it still differs from ShiftWizard's self-serve model. |
| Body | "SimpleScheduleAI is a new service, without public G2 or Capterra reviews yet." | JUDGMENT | "SimpleScheduleAI is new software, without public G2 or Capterra reviews yet." | Noun swap. |
| Bullet | "Evaluation on real delivered schedules, not demos. We build a real schedule against your roster so you judge the output" | JUDGMENT | "Evaluation on real generated schedules, not demos. We build a real schedule against your roster in the interactive demo so you judge the output" | "Delivered" (service-fulfillment verb) replaced with "generated"; folded in interactive-demo founder wording since the sentence already promised a hands-on preview. |
| Bullet | "Founder-led service. ... Service decisions go through him" | JUDGMENT | "Founder-led company. ... Product decisions go through him" | "Service"/"service decisions" recast to product-mode nouns; no registry row covers this exact construction (GAP). |
| Bullet | "CAH-specific design. Excel roster onboarding, no IT integration required..." | TOKEN:no-it | "...no installation or IT integration required..." | Aligned to `no-it` token's "name what is absent" rule (installation + integration, not a bald "No IT"). |
| Closing body | "For more on the operating model, see how SimpleScheduleAI works" | unchanged | unchanged | Not positioning-bearing; left as-is. |
| "Our Take" box | "ShiftWizard and a managed scheduling service answer different questions... A managed service answers 'how does the nurse manager stop being the scheduling administrator?'" | JUDGMENT | "ShiftWizard and a guided-setup scheduling software answer different questions... A guided-setup software answers 'how does the nurse manager stop being the scheduling administrator?'" | Category noun swapped throughout; rhetorical structure preserved (untouched per no-rewrite-beyond-positioning instruction). |
| What to Do This Week, item 1 | "Compare the total to the 1 to 2 hours per week a managed service requires." | JUDGMENT | "...a guided-setup software requires." | Noun swap. |
| What to Do This Week, item 4 | "A managed service is the right answer for managers who want the construction work removed." | JUDGMENT | "A guided-setup software is the right answer for managers who want the recurring construction work automated." | "Removed" (implies a human doing it elsewhere) changed to "automated" to avoid reintroducing an implied back-office team. |
| What to Do This Week, item 5 | "The managed service delivers the scheduling outputs ShiftWizard was purchased to enable, without the configuration maintenance burden." | JUDGMENT | "The guided-setup software delivers the scheduling outputs ShiftWizard was purchased to enable, without the ongoing configuration maintenance burden." | Noun swap. |
| CTA block heading | "ShiftWizard configuration without ShiftWizard configuration" | JUDGMENT | "ShiftWizard-grade rules without ShiftWizard-grade configuration" | Original heading was confusing/circular; tightened while preserving the "no ongoing config" claim central to product mode. Flagged as a rewrite beyond strict token swap — founder should confirm wording. |
| CTA subtitle | "SimpleScheduleAI provides ShiftWizard-grade compliance and fairness logic without requiring the nurse manager to maintain the system." | JUDGMENT | "...without requiring the nurse manager to maintain the system every cycle." | Minor clarity addition; "every cycle" ties to the recurring-maintenance distinction that is the crux of the product-mode reframe. |
| FAQ 2 | "With a managed service like SimpleScheduleAI, a pre-replacement call list ranked by certification..." | JUDGMENT | "With a guided-setup software like SimpleScheduleAI..." | Noun swap. |
| FAQ 3 | "A managed service bundles both into a service fee; a platform separates them." | TOKEN:pricing-short (adjacent) | "A guided-setup software bundles both into a flat facility license; a self-serve platform separates them." | "Service fee" recast using the `pricing`/`pricing-short` "flat facility license" framing already established in the registry's product-mode pricing row. |
| FAQ 4 | "a separate communication tool... alongside the managed service, rather than running two scheduling systems" | JUDGMENT | "...alongside SimpleScheduleAI, rather than running two scheduling systems" | Noun swap; named the product directly instead of the generic category noun since this FAQ is about SSAI specifically. |
| Closing link text | "See how the managed service model works in practice" | JUDGMENT | "See how the guided-setup model works in practice" | Noun swap. |
| Sources / Methodology note | unchanged | unchanged | unchanged | Factual/competitor content, untouched per instructions. |

**Post 1 counts:** Token hits: 6 (`self-label` x3, `triad` x2, `no-it` x1; `onboarding` and `pricing-short` framing referenced adjacently but not verbatim-quoted so counted under JUDGMENT below where paraphrased). Judgment calls: 20. Gaps (no token, recurring construction worth a registry row): 2 ("managed service" as external-specialist-team framing used repeatedly across headings; "service decisions go through him" founder-authority framing). Premise-collapse: 1 (title/thesis).

**Title/Slug Recommendation:** The post's entire argument is "self-serve platform (ShiftWizard) vs. managed scheduling service (category, not just SSAI)." In product mode, SSAI itself is no longer the managed-service example, but the category of "managed scheduling services" still exists in the market (other vendors), so the post's general framework is not fully broken — only the SSAI-as-example passages needed conversion (done above). Recommend: retitle to **"ShiftWizard vs. Guided-Setup Nurse Scheduling Software for Critical Access Hospitals (2026)"**, with the body's general "operating model" argument reframed as "self-serve platform vs. guided-setup software" (done in this dry-run), and a 301 from the old slug `/blog/shiftwizard-vs-managed-service` to a new slug `/blog/shiftwizard-vs-guided-setup-software` (or keep the old slug and only change the H1/meta title, avoiding a redirect, if SEO equity on the exact-match slug matters more than title accuracy — founder decision).

---

## Post 2: m7-health-vs-simplescheduleai.md

This is the worst case: the live title's own thesis ("Two Different Models: Managed Service vs. Software You Run Yourself") is service-vs-software, and SSAI is the service side. In product mode SSAI is software too, so the entire spine of the post (four H2s, the Key Takeaways, both tables, the FAQ) is built on a distinction that no longer holds as stated. This required section-level premise resolution, not just phrase swaps: the surviving distinction is "software you integrate and operate yourself" vs. "software set up in one guided session," which preserves the post's real informational content (IT burden, who configures, who maintains) without the now-false service/software binary.

| Location | Original phrase | Action | Replacement | Note |
| --- | --- | --- | --- | --- |
| Title | "M7 Health vs SimpleScheduleAI: Which Fits a Critical Access Hospital? (2026)" | Left unchanged (not premise-dependent itself) | unchanged | The vendor-vs-vendor title survives mode conversion fine; it is the internal H2 "Managed Service vs. Software" framing that collapses, not this outer title. |
| Excerpt | "SimpleScheduleAI is an AI-native nurse scheduling service built for the single Critical Access Hospital with no IT department." | TOKEN:self-label | "SimpleScheduleAI is AI-native nurse scheduling software built for the single Critical Access Hospital with no IT department." | Direct token. |
| KT bullet 2 | "SimpleScheduleAI is an AI-native nurse scheduling service built for the single Critical Access Hospital..." | TOKEN:self-label | "...AI-native nurse scheduling software built for..." | Direct token. |
| KT bullet 5 | "whether the hospital has the IT and scheduling staff that software you run yourself assumes" | JUDGMENT | "whether the hospital has the IT and scheduling staff that enterprise integration-heavy software assumes" | Reworded to keep the real contrast (integration burden) without implying SSAI is not also software. |
| TOC + H2 | "Two Different Models: Managed Service vs. Software You Run Yourself" | PREMISE-COLLAPSE | "Two Different Models: Guided-Setup Software vs. Software You Integrate Yourself" | Direct application of runbook Pass-2 rule 2 ("comparison content where service-vs-software is the argument itself... needs a rewrite, not a token swap"). New heading preserves the operational contrast (who integrates/operates vs. one guided session) while both sides are honestly "software." Anchor updated in TOC to match exactly. |
| Body under that H2 | "M7 Health is software the hospital runs itself: the organization licenses the platform, integrates it, and has its own staff operate it day to day. SimpleScheduleAI is an AI-native nurse scheduling service: the hospital sends a roster and the service builds and maintains the schedule." | PREMISE-COLLAPSE | "M7 Health is software a health system licenses and integrates with its own IT staff. SimpleScheduleAI is AI-native nurse scheduling software set up in a guided session: the hospital sends a roster and the system builds and maintains the schedule automatically after that." | Full-paragraph rewrite; the binary "software vs. service" replaced with "integrate-yourself vs. guided-setup," which is the actual operational claim underneath the old service language. |
| Body | "Software you run yourself fits an organization that has people to run it... A managed service exists for exactly that situation: it removes setup, configuration, and weekly maintenance so the hospital reviews finished schedules instead of running a platform." | PREMISE-COLLAPSE | "Software you integrate and operate yourself fits an organization that has people to run it... A guided-setup software exists for exactly that situation: it replaces the integration project and the weekly maintenance burden with one onboarding session, so the hospital reviews generated schedules instead of running an integrated platform." | Same rewrite pattern; "removes... so the hospital reviews finished schedules instead of running a platform" specifically implied an ongoing external operator, which the `no-it` founder rule forbids implying as solo-configuration's opposite (a service team) — reframed as automation after one session. |
| Body | "Running software yourself also means owning it over time... The question is not whether the software is capable. The question is whether the hospital can staff the role the software assumes." | JUDGMENT | "...The question is whether the hospital can staff the integration and maintenance role the software assumes, or needs a system built to require none of that." | Extended the closing sentence so it doesn't dead-end on "software you run yourself" as the only path; product mode needs the alternative named explicitly. |
| H2 "Who Is M7 Health Best For?" body | "software you run yourself is the correct model" | JUDGMENT | "software you integrate and run yourself is the correct model" | Consistency with the new H2 framing above. |
| H2 "Who Is SimpleScheduleAI Best For?" body | "She does not have eight hours a week to run scheduling software, and the hospital has no IT staff to configure it. The managed model fits that reality because the hospital reviews finished schedules instead of operating a platform." | PREMISE-COLLAPSE | "She does not have eight hours a week to run scheduling software, and the hospital has no IT staff to integrate it. The guided-setup model fits that reality because the hospital reviews generated schedules instead of operating and integrating a platform." | Same pattern: "managed model" (implies a human-staffed service) is not available in product mode; "guided-setup model" is the sanctioned replacement per registry `no-it` guidance. |
| Same section | "which is exactly the gap a managed service is built to close... SimpleScheduleAI is not the right fit, and software you run yourself may serve you better" | JUDGMENT | "...which is exactly the gap this kind of software is built to close... SimpleScheduleAI is not the right fit, and software built for that scale may serve you better" | Noun swap; avoided repeating "software you run yourself" a third time in near succession (repetition guardrail). |
| Same section, financial para | "A scheduling product that requires ongoing staff labor to operate adds cost in the form of nurse manager time... A managed service removes the labor cost entirely, not just the software cost." | PREMISE-COLLAPSE | "A scheduling product that requires ongoing IT and staff labor to integrate and operate adds cost in the form of nurse manager and IT time... Software built to run without an integration project removes that labor cost, not just the license cost." | "Managed service removes the labor cost" implied a service replacing labor with more labor (a vendor team); in product mode the honest claim is that automation removes the integration/operation burden, not that a hidden team absorbs it. |
| H2 "What Does Getting Started Look Like for a 25-Bed Hospital?" | "getting started with enterprise scheduling software and getting started with a managed service require entirely different things" | JUDGMENT | "...and getting started with a guided-setup software require entirely different things" | Noun swap; heading itself was mode-neutral and left as-is. |
| Body | "With SimpleScheduleAI, the hospital sends an Excel file with its staff roster. The service builds the first schedule draft from that. There is no payroll integration, no EHR connection, and no configuration the hospital owns." | TOKEN:onboarding + JUDGMENT | "With SimpleScheduleAI, the hospital sends an Excel file with its staff roster and completes a guided setup session that maps unit rules once. The software builds the first schedule draft from that. There is no payroll integration, no EHR connection, and no ongoing configuration the hospital owns." | Folded in the `onboarding` token's guided-setup framing; "the service builds" recast to "the software builds"; added "ongoing" to "no configuration" since a one-time guided session does happen (avoids overclaiming zero setup, which would itself be a `no-it` founder-rule violation in reverse — implying no guided session at all). |
| Body | "a CAH with no IT staff does not need to ask who will own the integration, because there is no integration" | JUDGMENT | "...because there is no integration, only a one-time guided setup session with the nurse manager" | Same reasoning: must not silently drop to "no configuration whatsoever," which would contradict the founder rule against implying solo/no-touch rule setup. |
| H2 "What Happens When a Nurse Calls Out at 5:30 a.m.?" | "With SimpleScheduleAI, the nurse manager contacts the service and receives a ranked replacement list... The documentation... is handled by the service." | TOKEN:callout | "With SimpleScheduleAI, the nurse manager opens the system and receives a ranked replacement list... The documentation... is handled automatically by the software." | Direct application of `callout` token's product-mode framing (system-driven, not "contact the service"). |
| Body | "the difference between logging it yourself in a platform at 5:30 a.m. and having it logged for you is real" | JUDGMENT | "...and having it logged automatically is real" | "Logged for you" implied a human service; "automatically" is the honest product-mode mechanism. |
| H2 "How Does SimpleScheduleAI Compare to M7 Health?" intro | "SimpleScheduleAI is a service that runs scheduling on behalf of one small hospital." | PREMISE-COLLAPSE | "SimpleScheduleAI is software set up in a guided session that then runs scheduling automatically for one small hospital." | Core sentence rewrite. |
| Table row "Delivery model" | "Fully managed scheduling service" | TOKEN-adjacent JUDGMENT | "Guided-setup software, no integration project" | Table cell reframed as a software capability per instructions ("SSAI rows/cells re-framed as software capabilities"). |
| Table row "IT and setup burden" | "None; Excel roster submitted by email" | JUDGMENT | "None; Excel roster upload plus a guided setup session" | Added the guided-setup session so the cell doesn't imply zero-touch configuration (founder rule). |
| Table row "Who does scheduling work" | "The service builds it; nurse manager reviews and approves" | JUDGMENT | "The software builds it automatically; nurse manager reviews and approves" | Noun swap. |
| Table row "Pricing" | "Flat monthly managed-service pricing" | TOKEN:pricing-short | "Flat monthly facility license by roster size" | Direct application of registry `pricing`/`pricing-short` product-mode framing. |
| H2 "Why Does Enterprise Scheduling Software Struggle at a Critical Access Hospital?" | "it is the central reason a managed model exists" | JUDGMENT | "it is the central reason guided-setup software exists" | Noun swap. |
| Same section, closing | "It is whether the hospital can staff the model the product assumes." | JUDGMENT | "It is whether the hospital can staff the integration and maintenance model the product assumes, or needs a system that requires none of that." | Extended for the same reason as the earlier "staff the role" edit: product mode needs the no-integration alternative stated, not just implied by omission. |
| H2 "How Should a Critical Access Hospital Evaluate a Scheduling Vendor?" | "who runs the system day to day... The product with the longest feature list is not the right one if no one at the hospital has time to operate it." | JUDGMENT | "...The product with the longest feature list is not the right one if no one at the hospital has time to integrate and operate it." | Minor addition of "integrate" to keep the IT-burden thread consistent with the reframed thesis. |
| Same section, Q1 | "This one answer separates software you run yourself from a service that runs scheduling for you" | PREMISE-COLLAPSE | "This one answer separates software you integrate and run yourself from software built to run without ongoing hospital operation" | Direct rewrite of the binary. |
| CTA block subtitle | "No IT, no configuration, no enterprise rollout." | TOKEN:no-it | "No installation, no integration project, no IT department required." | Direct application of the `no-it` token's exact founder-mandated language (never bald "No IT"; name what's absent). |
| Closing line before Sources | "see how AI nurse scheduling works as a managed service for small hospitals" | JUDGMENT | "see how AI nurse scheduling works as guided-setup software for small hospitals" | Noun swap. |
| FAQ 1 | "SimpleScheduleAI is the closer fit because it is an AI-native nurse scheduling service that needs no IT, no scheduler, and no configuration." | TOKEN:self-label + TOKEN:no-it | "SimpleScheduleAI is the closer fit because it is AI-native nurse scheduling software that needs no IT, no scheduler, and no integration project." | Combined tokens; "no configuration" changed to "no integration project" for the same overclaim-avoidance reason as above (a guided setup session does occur). |
| FAQ 4 (title + body) | "Why does SimpleScheduleAI use a managed service model instead of software?" / "A managed service removes setup, configuration, and maintenance, so the hospital sends a roster and reviews finished schedules instead." | PREMISE-COLLAPSE | "Why does SimpleScheduleAI use a guided-setup model instead of an integration project?" / "A guided-setup model removes the integration project, so the hospital sends a roster, completes one setup session, and reviews generated schedules from then on." | This FAQ question is only coherent in service mode (it presupposes "service instead of software" as the choice). Rewrote both Q and A so the real question — why no IT integration project — survives; TOC/anchor updated to match. |
| FAQ 5 title + body | "How do I compare an enterprise platform to a managed service fairly?" | JUDGMENT | "How do I compare an enterprise platform to a guided-setup software fairly?" | Noun swap in question and pointed link text ("guided-setup scheduling software versus enterprise scheduling software"). |
| Sources / Methodology / other FAQs | unchanged | unchanged | unchanged | Competitor facts (M7 figures, reference customers, ratings) untouched per instructions. |

**Post 2 counts:** Token hits: 9 (`self-label` x3, `no-it` x3, `callout` x1, `pricing`/`pricing-short` x2, `onboarding` x1 — some rows counted once even where a token's language was blended with judgment prose; see notes). Judgment calls: 24. Gaps (no token yet, but a recurring construction that likely deserves one): 3 ("software you run/integrate yourself" as the enterprise-model descriptor; "who does scheduling work" table-row framing; "reviews generated/finished schedules" as the manager's-role description). Premise-collapse: 8 distinct locations (the H2 itself, its two body paragraphs, the "Who Is SimpleScheduleAI Best For" passage, the financial-reality paragraph, the "How Does SimpleScheduleAI Compare" intro, the evaluate-vendor Q1, and FAQ 4 in full).

**Title/Slug Recommendation:** The outer title ("M7 Health vs SimpleScheduleAI: Which Fits a Critical Access Hospital?") does not itself assert a service-vs-software premise and can stay as-is. However, the post's internal architecture is built entirely around an H2 literally named "Managed Service vs. Software You Run Yourself" — that H2, its TOC anchor, and one FAQ question/answer pair do not survive product mode as written and were rewritten above (not just token-swapped). Recommend no slug change (the vendor-vs-vendor URL is still accurate and valuable for SEO), but flag for founder review: the rewritten H2 ("Guided-Setup Software vs. Software You Integrate Yourself") changes the anchor `#two-different-models-managed-service-vs-software-you-run-yourself` to `#two-different-models-guided-setup-software-vs-software-you-integrate-yourself`. If this post is migrated live, any external link or internal cross-link to the old anchor breaks and needs updating (checked: no other live post in this repo links to that specific anchor fragment, only to the post's root URL).

---

## Post 3: ai-nurse-scheduling-vs-traditional.md

This post's outer thesis (AI vs. traditional/manual scheduling) is mode-agnostic and survives cleanly — "traditional" is Excel/paper, not a competitor's service model. The positioning-bearing material is entirely in one internal sub-comparison: "AI scheduling software" vs. "AI managed service" as two flavors of AI adoption, plus the delivery-model self-description passages. That sub-comparison needed the same PREMISE-COLLAPSE treatment as Post 2's H2, but scoped to fewer sections since the rest of the post is genuinely about AI-vs-manual, not service-vs-software.

| Location | Original phrase | Action | Replacement | Note |
| --- | --- | --- | --- | --- |
| KT bullet 2 | "AI scheduling, whether delivered as software or as a managed service, applies overtime, credential, and coverage rules automatically" | JUDGMENT | "AI scheduling software applies overtime, credential, and coverage rules automatically" | The "whether delivered as software or as a managed service" hedge is no longer needed once SSAI itself is software; simplified rather than converting the hedge into an equally awkward product-mode hedge. |
| KT bullet 5 | "SimpleScheduleAI pairs constraint-aware AI scheduling with a managed service model, delivering three schedule options... with no software for the nurse manager to install or configure." | TOKEN:no-it + JUDGMENT | "SimpleScheduleAI pairs constraint-aware AI scheduling with a guided setup session, delivering three schedule options... with no installation and no IT department required." | "No software... to install or configure" was already close to product mode but paradoxically said "no software" about a software product; corrected plus applied `no-it` token's exact founant-mandated phrasing. |
| TOC + H2 | "Is AI Scheduling Software or an AI Managed Service Better for a Small Hospital?" | PREMISE-COLLAPSE | "Is a Self-Configured AI Platform or a Guided-Setup AI Software Better for a Small Hospital?" | Direct application of runbook Pass-2 rule 2. The real distinction the post wants (who configures/operates vs. one-time guided setup) is preserved; "managed service" as a second AI-delivery category is retired since SSAI no longer occupies it. Anchor updated in TOC to match exactly. |
| Body under that H2 | "Software and a managed service both apply AI to the same constraint problem. The difference is who runs the system. With software, the AI lives in a tool the nurse manager has to learn, configure, and operate every cycle." | PREMISE-COLLAPSE | "Self-configured and guided-setup software both apply AI to the same constraint problem. The difference is who does the configuration. With self-configured software, the AI lives in a tool the nurse manager has to learn, configure, and maintain every cycle." | Full paragraph rewrite; binary category names replaced with the configuration-burden framing. |
| Body | "A managed service applies the same AI on the hospital's behalf and maintains the configuration as part of the service." | PREMISE-COLLAPSE | "A guided-setup software maps the rules once in an onboarding session and then applies the same AI automatically on the hospital's behalf." | "Maintains the configuration as part of the service" implied an ongoing external team; product mode's honest claim is a one-time guided session plus automation, per the `no-it` founder rule. |
| Body | "the worthwhile comparison is Excel against a managed service, not Excel against more software" | JUDGMENT | "the worthwhile comparison is Excel against a guided-setup software, not Excel against more self-configured software" | Reworded so both sides of the comparison are honestly "software," differentiated by configuration burden rather than a service/software binary. |
| H2 "How Do Traditional and AI Scheduling Compare at a 25-Bed Hospital?" body | "AI scheduling software cuts that to 4 to 6 hours but still requires her to operate the tool. An AI managed service reduces it to 1 to 2 hours" | PREMISE-COLLAPSE | "Self-configured AI scheduling software cuts that to 4 to 6 hours but still requires her to configure and operate the tool. A guided-setup AI software reduces it to 1 to 2 hours" | Same self-configured/guided-setup distinction applied to this comparison paragraph. |
| Same section | "operational estimates from SimpleScheduleAI's work with Critical Access Hospitals" | GAP-NO-TOKEN | "operational estimates from SimpleScheduleAI's design work for Critical Access Hospitals" | Flagged separately below — see No-Customers Guardrail note. |
| Workflow diagram card heading | "AI Managed Service" (card label + badge) | JUDGMENT | "Guided-Setup AI Software" | Visual/diagram label; same conversion as body prose. |
| Comparison table header | "Managed Service (AI)" column header | JUDGMENT | "Guided-Setup Software (AI)" column header | Table header re-labeled; per registry rule 3 caution on positioning claims in table headers, this is a category label (not a claim like "we build/check/deliver") so it was converted directly rather than flagged as a rule violation. |
| Table row "System maintenance" | "Service" (cell value for the managed column) | JUDGMENT | "Handled by the software" | Cell reframed as a software capability per instructions. |
| Body para after table | "the time and error reduction is small from Excel to software, and large from Excel to a managed service, because software still leaves construction with the manager while a service removes it" | PREMISE-COLLAPSE | "the time and error reduction is small from Excel to self-configured software, and large from Excel to a guided-setup software, because self-configured software still leaves construction with the manager while a guided-setup software automates it after one onboarding session" | Full sentence rewrite; "a service removes it" (implies a human team doing the work) recast as automation. |
| H2 "What Does the Manual-to-AI Time Gap Cost a 25-Bed Hospital?" | "Take the gap between Excel and a managed service in the table" | JUDGMENT | "Take the gap between Excel and a guided-setup software in the table" | Noun swap. |
| Limitations section | "the most common failure mode at small hospitals that buy software and have no one to configure it, a pattern we examine in managed service vs. scheduling software" | JUDGMENT | "the most common failure mode at small hospitals that buy self-configured software and have no one to configure it, a pattern we examine in guided-setup vs. self-configured scheduling software" | Noun swap + updated the cross-link anchor text (the linked post itself, `/blog/managed-service-vs-scheduling-software`, is out of scope for this dry-run and was NOT converted; link target left as the live URL, only the anchor/link text was reworded — flagged below as an unresolved cross-link dependency). |
| H2 "How Does SimpleScheduleAI Use AI for Critical Access Hospital Scheduling?" | "applies constraint-aware AI scheduling to the specific operating reality... then delivers it as a managed service so the nurse manager never has to operate the system." | PREMISE-COLLAPSE | "...then runs it through a guided setup session so the nurse manager never has to configure the system herself." | Core sentence rewrite; "delivers it as a managed service" (service-fulfillment framing) replaced with the guided-setup mechanism. |
| Body | "SimpleScheduleAI is a nursing scheduling service and does not extend to physician or mid-level provider scheduling" | TOKEN:self-label (adjacent) | "SimpleScheduleAI is nursing scheduling software and does not extend to physician or mid-level provider scheduling" | Noun swap consistent with self-label token. |
| Body | "a managed AI scheduling service is not the right fit for hospitals over 50 beds... or organizations that specifically want to operate AI software under their own control rather than approve outputs." | PREMISE-COLLAPSE | "a guided-setup AI scheduling software is not the right fit for hospitals over 50 beds... or organizations that specifically want to configure and operate AI software fully under their own control." | Rewrite; "rather than approve outputs" trimmed since it restated the manager's-role point already made earlier (redundancy, not a positioning issue, but tightened while touching the sentence anyway). |
| Body | "the trade-off between operating a tool and approving a finished schedule is laid out in full" | JUDGMENT | "the trade-off between self-configuring a tool and approving a finished schedule is laid out in full" | Minor precision edit consistent with the self-configured/guided-setup distinction used throughout this post's converted sections. |
| What to Do This Week, item 3 | "Decide whether you want AI scheduling tools or an AI scheduling service. Software gives the manager better tools but still requires her to use them. A managed service applies the AI on her behalf." | PREMISE-COLLAPSE | "Decide whether you want AI scheduling software you configure yourself or a guided-setup AI software. Self-configured software gives the manager better tools but still requires her to set them up and maintain them. A guided-setup software maps the rules once and applies the AI automatically after that." | Full rewrite of the action item; same binary retirement as the H2 above. |
| FAQ 5 | "AI scheduling software typically needs 4 to 14 weeks of configuration... A managed service like SimpleScheduleAI needs only an Excel roster and usually delivers the first schedule within 48 hours... The gap reflects who does the configuration work: hospital staff in the software model, the service in the managed model." | PREMISE-COLLAPSE | "Self-configured AI scheduling software typically needs 4 to 14 weeks of configuration... A guided-setup software like SimpleScheduleAI needs only an Excel roster and one guided setup session, and usually delivers the first schedule within 48 hours... The gap reflects who does the configuration work: hospital staff in the self-configured model, a single guided session in the other." | Full rewrite; "the service in the managed model" (implies an ongoing vendor team doing configuration) replaced with the one-time guided session, consistent with the `no-it` founder rule. |
| Sources / other FAQs / competitor references | unchanged | unchanged | unchanged | Not positioning-bearing (ANA citation, FLSA/CMS facts, nurse-experience section) or out of scope (other competitors not mentioned in this post). |

**Post 3 counts:** Token hits: 3 (`no-it` x2, `self-label` x1). Judgment calls: 12. Gaps (no token, recurring construction worth a registry row): 2 (the "self-configured vs. guided-setup" binary itself, used as the retirement pattern for "software vs. managed service" — this is the single most repeated pattern across all three posts and is the strongest single candidate for a new registry row; "design work for/with Critical Access Hospitals" as the no-pilot-safe way to describe SSAI's operational estimates). Premise-collapse: 9 distinct locations (the H2 + TOC anchor, the body paragraph defining the two models, the "maintains the configuration" sentence, the "worthwhile comparison" clause, the hours-comparison paragraph, the post-table summary sentence, the SimpleScheduleAI-methodology sentence, the "not the right fit" limitations sentence, the What-to-Do-This-Week item 3, and FAQ 5 in full — note this totals 10 named spots across the paragraph-level; logged as 9 distinct "collapse points" grouping the two adjacent H2-body sentences together).

**Title/Slug Recommendation:** No change needed. "AI vs. Traditional Scheduling" is a category-level comparison (AI-assisted vs. manual/Excel) that has nothing to do with the service/software delivery model, so the post's premise is intact in product mode. Only the internal "software vs. managed service" sub-argument (one H2, its TOC anchor, and one FAQ) needed rewriting, and both were are handled above without any title, slug, or root-URL implication.

---

## Cross-post observations

1. **A new registry row is needed now, not later.** All three posts independently reinvent the same replacement for "managed service" as a second AI/software delivery model: some variant of "self-configured / self-serve platform vs. guided-setup software." This recurred at Post 1's "Managed Scheduling Service" category, Post 2's "Software You Run Yourself" H2, and Post 3's "AI Managed Service" H2 — three different posts, same missing token. Recommend adding a `delivery-model-binary` row to the mapping table pairing "software you run/operate/configure yourself" (or "self-serve platform") against "guided-setup software," so future conversions do not re-derive this phrasing by hand each time.

2. **The `no-it` token's "never imply solo rule-configuration" clause fired constantly and is under-enforced by any script today.** Nearly every JUDGMENT and PREMISE-COLLAPSE row above required re-inserting "guided setup session" or "one-time onboarding" specifically to avoid the false claim that product-mode SSAI configures itself with zero human input. A naive find-and-replace of "managed service" to "software" (or "our team checks it" to "checks it") would have silently produced exactly the anti-pattern the founder rule warns about. This is a judgment-heavy correction, not a regex-safe one; a `--dry-run` script per the runbook's Pass 1 would flag the trigger but a human still has to add back the guided-setup clause by hand every time, which is real ongoing cost, not a one-time migration cost.

3. **Post 2 shows the runbook underestimates how much of a "worst case" post is actually premise-collapse versus judgment.** The runbook's Pass 2 rule 1 anticipates "posts with managed service in the title" as the flagged category, but this post's title (`M7 Health vs SimpleScheduleAI`) does NOT contain that phrase — the collapse is buried in an internal H2 two screens down, invisible to a title-based or slug-based triage pass. A grep for "managed service" in titles/slugs alone would have missed this post's most serious problem entirely; the registry's own Pass 2 rule 1 example list needs to also flag internal headings, not just titles/slugs.

---

## Post-conversion facts scan

Command run:

```
node --input-type=module -e "import fs from 'node:fs'; const {checkFacts}=await import('./scripts/lib/facts-rules.mjs'); for(const f of fs.readdirSync('docs/seo/migration-dryrun')){ if(!/\.md$/.test(f)||f==='DECISIONS.md')continue; const v=checkFacts(fs.readFileSync('docs/seo/migration-dryrun/'+f,'utf8')); if(v.length)console.log(f,JSON.stringify(v)); }"
```

Result: **0 violations** across all 3 converted files. Confirmed clean.

## Leftover grep (self-label / service-language residue)

Searched all 3 converted `.md` outputs (excluding this file) for: `managed service`,
`our scheduling team`, `we build`, `No IT.` (bald form), `live demo`.

| File | String | Line | Intentional? | Why |
| --- | --- | --- | --- | --- |
| shiftwizard-vs-managed-service.md | "A managed service runs the scheduling function for you" | frontmatter `excerpt`, line 10 | Intentional | This post's title and excerpt were flagged PREMISE-COLLAPSE and deliberately NOT silently rewritten, per the task instruction to propose a new title/note the implication rather than bury a broken premise. The excerpt describes the article's subject (ShiftWizard vs. the managed-scheduling-service category, which still exists as a market category even though SSAI itself is no longer an example of it). A title/slug recommendation is logged above; the excerpt should be rewritten together with the title if this migration goes live, not as an isolated token swap. |
| shiftwizard-vs-managed-service.md | "We build a real schedule against your roster in the interactive demo" | body, "What we offer instead" bullet | Intentional, but flagged for founder judgment | This is describing the one-time demo/simulator action (the product generates a sample schedule when a prospect tries it), not the forbidden recurring "we build your schedule every week" service-triad claim already replaced elsewhere (see `triad` token rows). Borderline: "we build" reads close to service language even in this narrower sense. Recommend rewording to "The simulator builds a real schedule against your roster" if this ships, to fully remove the first-person "we + build" construction. |
| m7-health-vs-simplescheduleai.md | (none) | — | — | Clean. |
| ai-nurse-scheduling-vs-traditional.md | (none) | — | — | Clean. |

No instances of `our scheduling team`, bald `No IT.`, or `live demo` (all converted to "interactive demo/simulator" per founder rule) remain in any of the 3 outputs.

## Founder review corrections (2026-07-04)
- M7 post: "integration-heavy"/"Integrate Yourself" framing replaced with the factual runs-itself axis (M7's own positioning) grounded in ICP reality (a 25-bed hospital has no IT/scheduling staff to operate enterprise software). H2 + TOC anchor updated in pair. Competitor-inflation guardrail applied.
- M7 excerpt: "pick the right fit" restored to "pick the right model" (the post's thesis word; founder-confirmed).
- ShiftWizard title: options A/B/C presented to founder; pending pick. A = vendor-vs-vendor retitle + 301; B = category retitle, keep slug; C = keep title (category survives as market term).
